return {
  enabled = true;
  EasyUnlock = true; -- Toggle to use easier recipe (Moly)
  SkipIncantations = false; -- Toggle to skip incantations and enable mod by default (Minimal Mode)
  StackPenaltyScalar = 1.0; -- Multiplier for the probability penalty of stacked boons (Higher = less likely to find stacked boons)
}
